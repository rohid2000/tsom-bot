# name: auto-pull-master

# on:
#   push:
#     branches:
#       - master

# jobs:
#   deployment:
#     runs-on: ubuntu-latest
#     steps:
#       - name: deploy
  
name: auto-pull-master

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Deploy to server
      run: echo "hello"